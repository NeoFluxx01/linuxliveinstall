#!/usr/bin/env bash
###############################################################################
# Portable Debian on ZFS — Full Installer (PLANNED)
#
# Status: Not yet implemented
# Priority: Next — closest to Kali (same Debian base, ~90% shared code)
#
# Key differences from Kali installer:
#   - Mirror: deb.debian.org instead of http.kali.org
#   - Suite: trixie (stable) instead of kali-rolling
#   - Desktop: task-kde-desktop instead of kali-desktop-kde
#   - No Kali tools packages
#   - ZFS: zfs-dkms (may need backports repo on stable)
#
# See configs/debian.conf for package lists and settings.
###############################################################################
echo "Debian ZFS installer is not yet implemented."
echo "See configs/debian.conf for the planned configuration."
exit 1
